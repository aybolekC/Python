import random  #helps generate random integers between a given set of integers

die1=random.randint(1,6)  # returns number between 1 and 6 everytime we run
print(die1)

die2=random.randrange(6)  # will return a value >=x and <y
print(die2)

die3=random.randint(1,6)  # will return a value >=x and <y
print(die3)
